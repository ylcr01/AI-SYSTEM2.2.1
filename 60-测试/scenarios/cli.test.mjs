import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import test from 'node:test';import { fileURLToPath } from 'node:url';import { gitRepo,tempDir,runNode } from '../helpers.mjs';const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..');const TASK=path.join(ROOT,'40-脚本/task.mjs');
test('CLI 准备→交付→验收完整闭环',t=>{const repo=gitRepo(t),stateRoot=tempDir(t);const prepared=runNode(TASK,['准备','--cwd',repo,'--intent','修复普通功能','--acceptance','功能正确','--scope','.','--state-root',stateRoot],{cwd:ROOT});assert.equal(prepared.status,0,prepared.stderr);const task=JSON.parse(prepared.stdout);fs.writeFileSync(path.join(repo,'target.txt'),'changed\n');const delivered=runNode(TASK,['交付','--task-id',task.taskId,'--state-root',stateRoot],{cwd:ROOT});assert.equal(delivered.status,0,delivered.stderr);assert.equal(JSON.parse(delivered.stdout).status,'waiting_acceptance');const accepted=runNode(TASK,['验收','--task-id',task.taskId,'--state-root',stateRoot,'--decision','通过'],{cwd:ROOT});assert.equal(accepted.status,0,accepted.stderr);assert.equal(JSON.parse(accepted.stdout).status,'accepted');});
test('系统完整检查通过',()=>{const result=runNode(path.join(ROOT,'40-脚本/check-system.mjs'),[],{cwd:ROOT});assert.equal(result.status,0,result.stderr);assert.equal(JSON.parse(result.stdout).ok,true);});
