import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import test from 'node:test';import { readProjectManifests } from '../../40-脚本/lib/manifest-reader.mjs';import { tempDir } from '../helpers.mjs';
test('Manifest Reader 使用确定性候选并提取 Node 事实',t=>{const root=tempDir(t);fs.writeFileSync(path.join(root,'package.json'),JSON.stringify({name:'x',packageManager:'pnpm@9',scripts:{test:'node --test',build:'vite build'},dependencies:{vue:'3'}}));fs.mkdirSync(path.join(root,'random'));fs.writeFileSync(path.join(root,'random','ignored.json'),'{}');const result=readProjectManifests({gitRoot:root});assert.equal(result.length,1);assert.equal(result[0].kind,'node');assert.deepEqual(result[0].frameworks,['vue']);assert.ok(result[0].checksHints.includes('test'));});
