import assert from 'node:assert/strict';import path from 'node:path';import test from 'node:test';import { normalizeRemote,pathContains,validateRegistry } from '../../40-脚本/lib/registry.mjs';
test('Remote 归一化',()=>assert.equal(normalizeRemote('git@github.com:Owner/Repo.git'),'github.com/owner/repo'));
test('路径包含关系阻止越界',()=>{assert.equal(pathContains('/tmp/root','/tmp/root/a'),true);assert.equal(pathContains('/tmp/root','/tmp/other'),false);});
test('Registry 检测 Remote 冲突和角色错误',()=>{const registry={templates:{schemaVersion:1,templates:[{id:'t',role:'web',enabled:true,repository:{canonicalRemote:'github.com/a/r'},localPathKey:'t',entrypoints:{agents:'AGENTS.md',manifest:'package.json'}}]},projects:{schemaVersion:1,projects:[{id:'p',enabled:true,localPathKey:'p',entrypoints:{agents:'AGENTS.md'},modules:[{id:'m',role:'server',localPathKey:'m',canonicalRemote:'github.com/a/r',templateId:'t'}]}]},localPaths:{}};const r=validateRegistry(registry);assert.ok(r.errors.length>=2);});
