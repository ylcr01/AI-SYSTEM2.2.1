---
scope: universal
lifecycle: active
triggers: [组合, 状态序列, 交互]
source: AI-SYSTEM V1.1 真实任务经验审计；2026-09-09 V3 后任务记录与代码复盘
---

# 用实际状态与依赖约束检验行为

单项功能正确，不代表连续操作或默认依赖路径正确。需要聚焦单测时，可从原故障的初始状态、动作和结果构造用例；只选择会改变本次判断的条件，不枚举完整组合矩阵。

## 可复用的项目样例

| 情况与来源 | 有辨别力的预期 | 不能由此推出 |
| --- | --- | --- |
| Workbench [修复悬浮时显示 Port 状态](codex://threads/01a07e99-da70-7b13-a420-45e8fd3572ac)中的大批量移动 | 保留已确认的仅外框策略，开始预览不复制图元，移动期间不逐个更新位置；项目现有 `large-batch-move-preview-session.spec.ts` 已覆盖大数量场景 | “复制图元成功”的测试不能判断复制是否符合需求；操作次数符合约束，也不能代表浏览器帧率已经实测 |
| Workbench [分析多选元素键盘移动异常](codex://threads/01a07fc6-728c-79c3-a61d-b53b49a2d787) | 旧预览等待清理时再次移动，旧回调不影响新会话，预览与可见性正确恢复；沿用图适配器现有相关单测 | 单次移动成功不能证明连续换向、取消或交接正确；模拟时序不能证明现场观感 |
| Workbench [调整信息驱动的布局变化](codex://threads/01a07f81-e69a-7aa0-9d66-4b1f71bf3ed1)中的整体外框 | 默认调度路径保留函数接收者约束，能捕获 `Illegal invocation`；参考 `svg-selection-presentation.spec.ts` 与实现中的默认调度包装 | 只在注入理想函数时通过，不能证明默认依赖路径可用；不必因此自动启动完整 E2E |
| Workbench [分析美化布局无效原因](codex://threads/01a07fb0-d492-7142-a108-99cf25bcb4df) | 候选选择、最终拦截和结果展示对“管线交叉”采用一致含义；参考 `pid-node-arrangement.ts` 的实际端口与折线路径计算 | 候选评分正确不代表最终拦截正确；一份参考图通过不代表所有布局可用 |

这些案例的修复与验证范围依据 2026-09-09 复盘的历史会话和关键代码，未在本次重新运行。它们说明怎样选择测试条件，不构成整个项目已通过验收的声明。项目路径及当前代码仍由项目入口和 Git 确认，不复制一套中央测试实现。

测试替身可以隔离无关依赖，但应保留本次故障依赖的调用约束、时序或数据语义。有必要新增回归时，优先复用现有入口和夹具，让旧错误确实能被发现；已有适当用例则直接复用。必要的试验和修正融入当前工作，按根入口的验证范围执行，不形成额外收尾阶段。

## 适用边界

上述具体交互来自同一个图编辑项目，只用于理解验证取舍，不能据此要求所有项目采用相同批量阈值、预览策略或测试组织。没有状态组合或依赖疑点时，无需读取本资料或新增测试；无论何时，测试都不代替业务事实和未知协议的确认。
