# AI 研发操作系统 V2.3.0 入口规则

## 路由

- **普通对话**：不依赖仓库事实时直接回答，不建 Task、不运行工程脚本。
- **只读工程分析**：只读`预检`后以 `build-context.mjs --operation read` 读最小事实；仅身份、路由或依赖诊断加 `--full`。
- **仓库写入**：先只读预检 Git 清洁度、分支身份和已知写入者，再用 `--operation write`（外写用 `external-write`）读取最小项目事实。当前工作区干净、可用且没有已知并发写入时，模型直接实施并展示真实 Git Diff；关键词、目录名和模型自行推测的文件范围不得自动创建 Task 或 Worktree。
- **模型自主执行**：普通修改不预建 Goal Card、Scope、Evidence、结果记录或本地提交；模型按用户目标、代码和调用方完成最小改动、相称验证及可安全自动化命令，不转交用户。用户限定文件时 Scope 是硬边界，否则以最终 ChangeSet 为准；只在需用户决策/授权或宿主无接口时请求介入。
- **Worktree**：只在工作区脏、被占用、存在已知并发、状态无法确认或用户明确要求隔离时使用 managed/detached Worktree，用于保护现有改动，不用于替代模型的风险判断。
- **正式写任务**：Task 是持续跟踪、跨对话交接、并行隔离或已授权外部写入的显式能力。调用 `准备` 即表示明确选择正式闭环；此时才使用专属 Worktree、Scope、Evidence、提交与集成门禁。不得从普通意图关键词或路径名称自动升级。
- **对话后续**：只有精确 continuation 才调用`后续`；否则不猜 Task、不存正文、不跑检查。
- **结果统计**：一问题一样本，完成轮次含交付、追问、缺陷和退回；旧口径、问答和只读不计。
- **目标理解**：先读用户请求、目标代码和直接调用方；仅在业务结果、权限或外部影响存在实质歧义时确认。普通任务不为形式完整生成目标卡，根因未知时先只读探索。详见 `20-能力模块/clarify-requirements/CONTRACT.md`。
- **外部/高风险写入**：默认不得 Push；Push、发布、部署、迁移、远程删除和生产数据修改须另获授权，不可逆动作还须明确停止与回滚。

## 可信边界

- 权威顺序：用户目标/授权 → 代码/配置/Manifest/Git → 已确认规格 → Contract/Canonical → 其他资料；冲突不静默选择。
- 仅在业务结果、用户明确文件边界、外部影响或权限会变化时确认；实施最小充分变更。
- Acceptance 只写结果，不写命令、通用检查或全量门禁；Planner 前只写“生成并执行最小验证计划”。普通交付仅跑定点检查和缺失 Cover；全量回归须独立 Task 和明确授权。
- `package.json` 不按路径升级；元数据不升级，新增/删除、未知差异或其他字段按 build-contract 升级。
- 默认用有界 `rg`、选段、`git diff --stat/--numstat` 和进程内聚合；不加载整份 Task JSON、对话历史、成功日志或大 Diff。裁剪不得隐藏首个失败、跳过、终止、Evidence、Blocker 和新鲜度。
- 不猜项目、权限或外部授权；普通修改以实际 ChangeSet 报告范围，用户明确限定的文件边界不得越过；保留用户改动。
- 显式 Quality 的 JSON/shape/路径/文件无效时 fail closed；Context 只返回授权根内可读文件。
- 正式 Task 的 Evidence 绑定 ChangeSet/输入周期/Acceptance/Covers。普通修改只如实报告执行过的检查，不建立 Evidence 状态。导入仅辅证；文档/contract/visual 直接证明须有 artifact、哈希和系统来源。
- 自检非独立审查；`accepted` 只由用户显式产生，`closed` 只表示对话收口。
- `诊断状态` 分报存储完整性和验收资格；项目身份按 `gitCommonDir`，交付尝试与检查执行分计。
- 交付前按 ChangeSet 声明 `specImpact=none|updated|decision-required`；后两者须过规格/Decision 门禁。

## 浏览器验证硬门禁

- 浏览器验证使用全局宿主入口的硬限制；该入口未加载时不得执行，并如实报告 `unimplemented`，其他检查不能替代。

## 按需规则

规格/Decision/经验读 `70-文档/25-按需任务规则.md`；中央机制增删改读 `70-文档/10-架构与原则.md`、`70-文档/55-系统演进准入.md`，仅净正向进入默认路径；其余不加载。行为保持见 `20-能力模块/clarify-requirements/CONTRACT.md`，普通优化/重构不自动严格保持。
