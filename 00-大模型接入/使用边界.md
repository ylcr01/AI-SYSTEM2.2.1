# 使用边界

自定义指令只能约束具备以下能力的宿主：

- 能读取本地文件；
- 能执行 Node.js/终端命令；
- 能在当前项目目录操作 Git 工作树。

普通网页聊天如果没有本地文件或终端能力，无法仅靠自定义指令调用本机 V2.2.1。自定义指令不会自动授予宿主额外权限。

V2.2.1 不使用常驻服务、MCP 网关或独立 Host Adapter；它通过短入口指令和根 `AGENTS.md` 驱动支持代码执行的模型宿主，因此保持轻量。
