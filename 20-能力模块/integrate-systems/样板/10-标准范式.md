# 外部适配器范式

```text
Business Port
  → Adapter
    → Auth / Mapping / Timeout / Retry / Idempotency
    → External System
  ← Internal Result / Internal Error
```

Adapter 记录非敏感的请求标识、耗时和外部错误分类；业务层只依赖内部稳定接口。
