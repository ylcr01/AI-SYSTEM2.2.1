# 服务端业务用例范式

```text
Transport
  → Application Use Case（权限、事务、流程）
    → Domain Rule
    → Repository / External Adapter
  ← 明确结果或错误
← Protocol Mapping
```

一个业务规则只有一个权威实现。更新和删除先确认目标、权限和并发条件。外部 DTO 不进入领域层。
