---
name: design-product
description: 产品定义、功能范围、用户流程和产品验收。
---

# design-product

先读取当前项目事实、目标代码和直接调用方。`none/local` 默认不加载 Canonical；`structural` 才读取一个主要 Contract 和最多一个 Active Canonical。工程先验是保底，不是唯一答案，模型可基于更强项目事实偏离。
