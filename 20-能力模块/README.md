# 可选工程资料

本目录中的 Contract 与样板是普通参考资料。模型根据当前用户目标和项目事实自主决定是否读取；中央 Profile、关键词路由和自动加载均已停用，旧 `manifest.json` 不参与当前运行。

项目事实高于中央参考。资料不得建立 Task、Scope、Evidence、自动检查或集成生命周期，也不得降低用户已有改动、明确范围、外部授权、默认不 Push 和结果真实性边界。
