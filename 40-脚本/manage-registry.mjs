import path from 'node:path';import { parseArgs,requiredArg } from './lib/args.mjs';import { atomicWriteJson } from './lib/atomic-file.mjs';import { loadRegistry,validateRegistry,normalizeRemote,SYSTEM_ROOT } from './lib/registry.mjs';const args=parseArgs(process.argv.slice(2));const command=args._[0]??'list';const root=path.resolve(args.root??SYSTEM_ROOT);const registry=loadRegistry(root);const dir=path.join(root,'10-注册表');function save(){const result=validateRegistry(registry);if(result.errors.length)throw new Error(result.errors.map(x=>`${x.location}: ${x.message}`).join('; '));atomicWriteJson(path.join(dir,'projects.json'),registry.projects,path.join(root,'80-运行记录','.pending'));atomicWriteJson(path.join(dir,'templates.json'),registry.templates,path.join(root,'80-运行记录','.pending'));atomicWriteJson(path.join(dir,'local.paths.json'),registry.localPaths,path.join(root,'80-运行记录','.pending'));}
try{if(command==='list')console.log(JSON.stringify({templates:registry.templates.templates,projects:registry.projects.projects,paths:registry.localPaths,validation:validateRegistry(registry)},null,2));else if(command==='validate'){const result=validateRegistry(registry);console.log(JSON.stringify(result,null,2));if(result.errors.length)process.exitCode=1;}else if(command==='set-path'){registry.localPaths[requiredArg(args,'key')]=path.resolve(requiredArg(args,'path'));save();console.log('路径已更新');}else if(command==='add-template'){const id=requiredArg(args,'id'),role=requiredArg(args,'role'),remote=normalizeRemote(requiredArg(args,'remote')),localPathKey=args['path-key']??`template.${id}`;registry.templates.templates.push({id,role,enabled:true,repository:{canonicalRemote:remote,allowedRemotes:[remote]},localPathKey,entrypoints:{agents:args.agents??'AGENTS.md',manifest:args.manifest??(role==='server'?'pom.xml':'package.json')},knowledge:{mode:'in-repo',root:'.ai',manifest:'.ai/manifest.json'},quality:{manifest:'.ai/quality.json'}});if(args.path)registry.localPaths[localPathKey]=path.resolve(args.path);save();console.log('模板已添加');}else if(command==='add-project'){const id=requiredArg(args,'id'),localPathKey=args['path-key']??`project.${id}`;registry.projects.projects.push({id,enabled:true,localPathKey,entrypoints:{agents:args.agents??'AGENTS.md',docs:args.docs??'README.md'},modules:[]});if(args.path)registry.localPaths[localPathKey]=path.resolve(args.path);save();console.log('项目已添加');}else if(command==='add-module'){const project=registry.projects.projects.find(x=>x.id===requiredArg(args,'project'));if(!project)throw new Error('项目不存在');const id=requiredArg(args,'id'),role=requiredArg(args,'role'),localPathKey=args['path-key']??`project.${project.id}.${role}`;project.modules.push({id,role,localPathKey,canonicalRemote:normalizeRemote(requiredArg(args,'remote')),...(args.subpath?{subpath:String(args.subpath).replaceAll('\\','/')}:{}) ,...(args.template?{templateId:args.template}:{})});if(args.path)registry.localPaths[localPathKey]=path.resolve(args.path);save();console.log('模块已添加');}else if(command==='set-enabled'){const id=requiredArg(args,'id');const item=[...registry.templates.templates,...registry.projects.projects].find(x=>x.id===id);if(!item)throw new Error('登记不存在');item.enabled=requiredArg(args,'enabled')==='true';save();console.log('状态已更新');}else throw new Error(`未知注册表命令: ${command}`);}catch(error){console.error(`注册表操作失败: ${error.message}`);process.exitCode=1;}
