# 可信门禁

正式进入 `waiting_acceptance` 必须同时满足：

- 项目和 Git Root 有效；
- Scope 在 Git Root 内；
- 用户已有改动未被未授权触及；
- ChangeSet 当前有效；
- Evidence Schema、Payload Hash、Acceptance 和 Covers 有效；
- Required Covers 和 Acceptance 均被证明；
- 无 Blocker 和 Blocking Review Finding；
- 用户/项目显式要求的 Review 已满足；
- handoff-required 任务存在新鲜 Handoff。
- linked/detached worktree 任务已经形成独立 `resultCommit`，且没有未提交的任务改动；
- 需要集成的任务已经确认：merge 后原 `resultCommit` 可达，或 cherry-pick 后全部任务补丁存在等价提交；确认时记录的目标提交当前仍可从声明的目标分支到达。

用户验收前重新执行上述校验。`accepted` 只能通过用户验收事件产生。

并行任务在 Evidence 和 Review 满足后先进入 `ready_to_integrate`。系统为其创建 `refs/ai/pending/<taskId>`，防止 detached commit 因 worktree 清理而丢失。单一集成者完成 cherry-pick 或 merge 后执行 `task.mjs 集成`；确认成功后清理 pending ref，并进入 `waiting_acceptance`。
